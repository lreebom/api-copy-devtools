# 更新日志

[English](./CHANGELOG.md) | [简体中文](./CHANGELOG.zh-CN.md)

API Copy 的重要变更记录在这里。

## 0.4.1 - 2026-09-23

- 修复请求参数中百分号编码中文的显示问题，覆盖 Query、URL 编码表单以及请求列表中可选显示的 URL 路径和 Query。
- 原始请求复制继续保留 URL 编码，确保复制出的地址可直接使用。

## 0.4.0 - 2026-09-23

- 增强 JSON Preview 的大数据处理，完整保留响应内容并按需创建树节点。
- 优化折叠对象摘要，按名称、编码、ID、状态优先展示，并使用可用行宽展示更多字段。
- 优化 JSON 树的紧凑布局、折叠箭头、Hover 高亮和数组范围标识。
- 新增 FormData 和 URL 编码表单参数展示，支持显示文件名及可用的文件元数据。
- 精简请求参数面板的空状态文案。

## 0.3.0 - 2026-09-16

- 新增可拖拽的请求参数区域，支持查看 URL Query 参数和请求 Body。
- 新增全局路径前缀截断，以及控制请求列表是否展示 URL Query 参数的选项。
- 请求列表新增序号，并在页面导航后清空已记录请求。
- 优化 JSON Preview：默认展开层级、命中项切换、展开状态保留、类型摘要与字符串展示。
- 优化响应式面板尺寸、标题栏、搜索控件和 JSON 布局。

## 0.2.0 - 2026-09-14

- 新增自动、左右和上下布局切换，并记住用户选择。
- 请求方法与状态改为上下两行紧凑列，分隔条简化为单线，Preview 字段值可以使用整行可用宽度。

## 0.1.0 - 2026-09-12

首次公开发布。

- 新增高频复制流程：路径、路径+参数/Body、精简请求、完整请求和 Token。
- 新增键盘导航和快捷键。
- 新增请求行 Hover 快捷复制操作。
- 新增基于 `isSuccess` 和 `code` 的业务失败标记。
- 新增可配置的 Query 参数过滤，默认过滤 `_t`。
- 新增可搜索、可排序的 JSON Preview，并优化长值显示和复制。
- 新增可拖拽、自适应的请求/响应面板。
- 常用操作按钮统一增加图标。
- 新增英文和简体中文界面，并自动跟随 Chrome 的界面语言。
