# 更新日志

[English](./CHANGELOG.md) | [简体中文](./CHANGELOG.zh-CN.md)

API Copy 的重要变更记录在这里。

## 0.1.0 - 2026-09-12

首次公开发布。

- 新增高频复制流程：路径、路径+参数/Body、精简请求、完整请求和 Token。
- 新增键盘导航和快捷键。
- 新增请求行 Hover 快捷复制操作。
- 新增基于 `isSuccess` 和 `code` 的业务失败标记。
- 新增可配置的 Query 参数过滤，默认过滤 `_t`。
- 新增可搜索、可排序的 JSON Preview，并优化长值显示和复制。
- 新增可拖拽、自适应的请求/响应面板。
- 常用操作按钮统一增加图标。
